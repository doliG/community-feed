Components are all dumbs (meaning they are pure functions) and are organized in two folders:
- `standalone/`: standalone which can work completely isolated
- `composed/`: components composed with other components
